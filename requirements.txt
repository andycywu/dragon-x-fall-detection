# 🐉 Dragon X Fall Detection System Dependencies
# Qualcomm Snapdragon X Elite優化版依賴包 (ARM64-optimized, wheel-friendly versions to avoid source compilation)

# Core AI Framework 核心AI框架
onnxruntime
onnxruntime-directml
onnxruntime-qnn 
# 以下加速器將由 install_platform_accelerators.py 自動安裝:
# - Windows: onnxruntime-directml (x64/ARM64)
# - Windows ARM64: onnxruntime-qnn (Snapdragon)
# - macOS ARM64: onnxruntime-coreml (Apple Silicon)
# - Linux ARM64: onnxruntime-qnn

# PyTorch - platform-specific versions
# 注意: 這裡提供適合各平台的版本
# 使用 install_platform_accelerators.py 會自動選擇最適合您平台的版本
torch>=2.0.0
torchvision>=0.15.0
# 不再使用 CPU-only 特定版本，使用 pip 自動選擇適合的版本
# --extra-index-url https://download.pytorch.org/whl/cpu

mediapipe==0.10.14

# Computer Vision 計算機視覺
opencv-python-headless==4.10.0.84
Pillow==9.5.0
numpy==1.23.5
scipy==1.10.1

# Web Interface and API Web界面和API
streamlit==1.28.0
requests==2.28.2
fastapi==0.95.1
uvicorn==0.22.0

# Speech Processing 語音處理
faster-whisper==1.0.0
ctranslate2==4.5.0
sounddevice==0.4.4
librosa==0.9.2

# Data Processing 數據處理
pandas==1.5.3
matplotlib==3.7.1
seaborn==0.12.2
plotly==5.14.1
transformers==4.29.2

# QAI Hub Client QAI Hub客戶端
qai-hub==0.9.0

# System Utilities 系統工具
psutil==5.9.4
tqdm==4.65.0
pyyaml==6.0

# Development Tools 開發工具
pytest==7.3.1
black==23.3.0
flake8==6.0.0
