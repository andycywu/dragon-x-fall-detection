############################################################
# Dragon X Fall Detection - Windows / Snapdragon X Elite 依賴清單
# 分層: 核心 → QAI Hub → 拓展(語音/視覺/Web) → 可選高風險
# 安裝策略: 先核心 + QAI Hub；其他視情況追加。
############################################################

# ========== 核心必備 ==========
numpy>=1.26.0
opencv-python>=4.9.0.80
Pillow>=10.2.0
requests>=2.31.0
prettytable>=3.10.0
python-dotenv>=1.0.1
# ========== PyTorch (CPU) ==========
# 若官方釋出 Windows ARM64 專用輪子，pip 會自動取得；
# 如需特定 index (僅 CPU): --index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# ========== QAI Hub 生態 ==========
qai-hub
qai-hub-models

# ========== Whisper / 語音 (可選) ==========
# 需要 ffmpeg（手動安裝並加入 PATH）
pyttsx3>=2.90
SpeechRecognition>=3.10.0
openai-whisper>=20231117

# ========== Web / 服務 (可選) ==========
# streamlit 用於展示；FastAPI + uvicorn 可做 API。
streamlit>=1.33.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ========== 數據/分析 (可選) ==========
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0

# ========== 進階 / 其他 (可選) ==========
scipy>=1.11.0
tqdm>=4.66.0
psutil>=5.9.0
pyyaml>=6.0.0

# ========== 高風險 / ARM64 可能失敗 ==========
# face-recognition
# dlib

############################################################
# 安裝建議:
# 1) 建立並啟用虛擬環境:
#    python -m venv venv
#    venv\Scripts\activate
# 2) 升級 pip:
#    python -m pip install --upgrade pip
# 3) 分段安裝 (失敗好排錯):
#    pip install -r requirements_windows.txt --no-cache-dir --extra-index-url https://download.pytorch.org/whl/cpu
# 4) 若要升級 QAI Hub SDK:
#    pip install --upgrade --upgrade-strategy eager qai-hub qai-hub-models
# 5) Whisper 需 ffmpeg:
#    確保 ffmpeg.exe 在 PATH (可用 ffmpeg -version 測試)
# 6) face-recognition 如需: 先安裝 CMake 與 dlib 再解除註解
############################################################
